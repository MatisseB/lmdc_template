{{! 404 Error page; this file renders a 404.html page; the "create404page" flag must be defined and enabled in config.json file within the "renderer" section }}
{{> head}}
{{> navbar}}
<main>
   <div class="error-page">
      <h1>{{ translate 'error.404' }}</h1>
      <p>{{ translate 'error.message' }}</p>
      <a href="{{@website.url}}" class="button">
         {{ translate 'error.button' }}
      </a>
   </div>
</main>
{{> footer}}
